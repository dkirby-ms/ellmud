# ⚠️ DEPRECATED WORKFLOW ⚠️
#
# This workflow has been deprecated in favor of automated version bumping.
#
# **New versioning model:**
# - Patch bumps: Automated on every dev → uat promotion
# - Minor bumps: Automated on every uat → prod promotion
# - Major bumps: Manual only (use `npm version major` on dev branch)
#
# **Workflows:**
# - `squad-promote.yml` — Manual promote with auto version bumping
# - `scheduled-uat-promote.yml` — Scheduled dev → uat with auto patch bump
# - `squad-release.yml` — Auto-tag and release on prod push
#
# **DO NOT USE THIS WORKFLOW** — It creates duplicate releases and bypasses
# the automated version bumping system.
#
# For reference, the original workflow is preserved below:
# (renamed from release.yml on 2025-01-XX)

name: Release (DEPRECATED - DO NOT USE)

on:
  workflow_dispatch:
    inputs:
      version:
        description: '⚠️ DEPRECATED - Use squad-promote.yml instead'
        required: true
        default: 'patch'
        type: choice
        options:
          - major
          - minor
          - patch

permissions:
  contents: write
  pull-requests: read

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: ⚠️ Workflow Deprecated
        run: |
          echo "::error::This workflow is deprecated. Use squad-promote.yml for automated version bumping."
          echo "::error::Patch bumps: Automated on dev → uat promotion"
          echo "::error::Minor bumps: Automated on uat → prod promotion"
          echo "::error::Major bumps: Manual npm version major on dev branch"
          exit 1
