# Orchestration Log — Drizzt

**Date:** 2026-03-21T23:05Z  
**Agent:** Drizzt (Engine Dev)  
**Wave:** Phase 1 Connection Resilience — Should-Fix Sprint

## Assigned Work (Item 4)

✅ **Consolidate extraction_state handler** — Fixed latent reconnect bug in `packages/client/src/hooks/useShardConnection.ts`

## Files Changed

- `packages/client/src/hooks/useShardConnection.ts` — Moved `handleExtraction` definition into `handlers` block, stored ref for reconnection callback access

## What Was Fixed

**Issue:** `extraction_state` message handler was only registered during initial `connect()`, missing from `switchRoom()` and reconnection paths.

**Solution:**
1. Moved `handleExtraction` to be co-located with all other handlers
2. Stored in `extractionHandlerRef` so reconnection callback (outside useEffect) can access it
3. Registered handler in all three room-creation paths: initial connect, switchRoom, and reconnection

## Decisions Documented

- **"Decision: extraction_state handler consolidated"** — Pattern for message handler registration. Trade-off: kept change scoped to one file per review constraint; future message types should extend MessageHandlers interface.

## Integration Notes

- Reconnection callback now properly re-wires extraction handler
- Pattern can be applied to other message types if MessageHandlers interface is extended
- No breaking changes to other hooks or components

## Status

✅ COMPLETED — Latent reconnect bug fixed, handler properly wired across all connection paths.
