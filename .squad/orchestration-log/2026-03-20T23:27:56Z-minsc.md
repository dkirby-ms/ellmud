# Wave 5 — Minsc (Tester)

**Task:** Anticipatory tests for client UI batch (#74, #75, #67)

**Outcome:** SUCCESS

**Work:**
- 100 tests across 3 suites: Button (40), Toast (35), ClickableExits (25)
- Import-failure pattern: import real components, tests fail at import until implementation lands
- Toast timer tests use `vi.useFakeTimers()` with `shouldAdvanceTime: true`; switch to `vi.useRealTimers()` for userEvent
- Toast cleanup via `toast._reset()` in beforeEach/afterEach
- ExitLink role="link" on span (tabIndex=0), not `<a>` element
- Button class checks use `.toContain('btn--primary')` (BEM convention)
- CSS variable compliance verified (no inline hex values)
- 100 tests passing
- All anticipatory tests active immediately upon feature branch merge

**Decision Inbox Entry:** minsc-client-ui-tests.md — archived to decisions.md

**Cross-Team Impact:**
- All three feature PRs (#84, #85, #86) have passing test suites validating API contracts
- Test patterns establish conventions for future component testing
- Import-failure pattern makes missing implementations obvious at CI time

**Status:** Anticipatory test suite locked, 100 tests passing on dev.
