# Orchestration: Drizzt — 2026-03-19T22:30

## Directive
Fixed static file serving for UAT environment.

## Completed Work
- **Dockerfile**: Multi-stage build now copies `packages/client/dist` → `packages/server/dist/public`
- **Express config**: Added `express.static()` middleware + SPA catch-all (`app.get('*')` → `index.html`) for React Router
- **Route order**: API routes (`/auth`, `/health`, `/admin`, `/colyseus`) registered BEFORE static middleware; SPA catch-all last
- **Tested**: Client loads in Azure UAT; API endpoints still callable
- **Commit**: 3a45dd0

## Decisions Made
- Decision: "Serve React Client from Express in Production" — documented in inbox → decisions.md
- Pattern: Server-side client serving + static file middleware + SPA catch-all is the approved pattern for Phase 1+

## Implications for Squad
- Any new API routes MUST be registered before the catch-all, or they'll be swallowed
- Static assets (`/public/*`) are now served from the runtime container, not a separate CDN
- Build dependency: client's Vite build must complete before server Docker build

## Next: Phase 2 UAT Testing
Ready for full end-to-end smoke test.
