# Agent: Jarlaxle | ACA Container Entrypoint Fix

**Date:** 2026-03-21T15:09:00Z  
**Task:** Fix ACA container entrypoint in CI/CD  
**Mode:** Background  
**Outcome:** ✅ SUCCESS

## Work Summary

Fixed Azure Container Apps deployment pipeline to properly clear bootstrap arguments and improve health check validation.

### Changes Made

1. **Bicep Template** (`infra/modules/container-apps.bicep`)
   - Added `--args ""` to `az containerapp update` command to clear inherited bootstrap args
   - Ensures deployed container runs real server, not placeholder bootstrap

2. **Health Check** (`infra/main.bicep`)
   - Changed validation from generic `"status":"ok"` to real server's `"uptime"` field
   - Prevents false-positive health checks masking failed deploys
   - Bootstrap placeholder and real server both return status=ok, but only real server has uptime field

3. **Code Comments**
   - Added clarifying comments to Bicep explaining two-phase module pattern
   - Documented why both command and args must be explicitly set

### Impact
- Applied to both UAT and Production environments
- Deployment workflow now verifies real server startup, not placeholder
- Commits pushed to dev branch

### Decision Record
Recorded in `.squad/decisions/inbox/jarlaxle-aca-entrypoint-fix.md`
