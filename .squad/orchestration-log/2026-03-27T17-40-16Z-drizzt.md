# Drizzt Orchestration Log

**Date:** 2026-03-27T17:40:16Z  
**Agent:** Drizzt (Engine Dev)

## Task

Fix DB constraint error in PgExplorationRepository and FK violations in ShardRoom player ID resolution

## Outcome

✅ **SUCCESS**

### Work Completed

1. **Changed ON CONFLICT ON CONSTRAINT to expression-based conflict detection**
   - Problem: Migration 032 creates UNIQUE INDEX, not table constraint
   - PostgreSQL ON CONFLICT ON CONSTRAINT requires named table constraint
   - Solution: Used ON CONFLICT (character_id, COALESCE(zone_slug, '__shard__'), room_id)
   - Now works with both unique indexes and table constraints

2. **Added authPlayerIds map to separate game state ID from DB persistence ID**
   - Game state uses characterId (client-provided string)
   - DB operations require authPlayerId (JWT token UUID from players.id)
   - ShardRoom.ts now tracks mapping: characterId → auth playerId UUID
   - All repository operations (skills, faction, run history) use auth player UUID
   - Mapping cleaned up in onLeave to prevent memory leaks

### Verification

- Build status: Clean ✓
- Full test suite: 2239 tests pass ✓
- No regressions detected
- All FK constraints satisfied (player_skills, run_history, character_explored_rooms)

### Decisions Logged

- **2026-03-20:** Database Constraint and FK Error Fixes
- **2026-03-27T16:32Z:** Stability bar and collapse timer deprecated (user directive)

## Files Modified

- `packages/server/src/exploration/PgExplorationRepository.ts` — ON CONFLICT fix
- `packages/server/src/rooms/ShardRoom.ts` — authPlayerIds mapping, repository call fixes
- Related schema files:
  - `packages/server/src/db/migrations/032_create_explored_rooms.sql`
  - `packages/server/src/db/migrations/017_create_characters.sql`
  - `packages/server/src/db/migrations/018_rekey_tables_to_character.sql`
