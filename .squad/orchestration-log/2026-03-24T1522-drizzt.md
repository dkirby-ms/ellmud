# Orchestration Log: Drizzt (Engine Dev)

**Timestamp:** 2026-03-24T15:22:00Z  
**Task:** Fix Redis ETIMEDOUT crash on uat  
**Mode:** background  
**Status:** ✅ Completed

## Outcome Summary

Created testRedisConnection() probe utility. Both createPresence() and RedisDriver init now probe first, fall back gracefully. All 3 new tests pass. All 1444+ tests passing. Committed 3a649e9. Created skill: redis-connectivity-probe.

## Files Modified

- packages/server/src/cache/redis-test.ts (new)
- packages/server/src/cache/redis-presence.ts
- packages/server/src/cache/index.ts
- packages/server/src/index.ts
- packages/server/src/__tests__/redis-test.test.ts (new)

## Key Decision

All Redis-dependent components must be **pre-validated** before construction. A shared `testRedisConnection()` probe tests connectivity with a short-lived, disposable ioredis client before handing the connection string to any library that doesn't handle failures gracefully.

**Pattern:** probe first → construct on success → fall back on failure

## Impact

- Server starts gracefully even when Redis is completely unreachable
- Clear log messages indicate fallback to local alternatives
- No new dependencies — uses ioredis which is already in the dependency tree
- All 1444+ tests continue to pass
- UAT ETIMEDOUT crash is now resolved

## Related

- Issue: Redis ETIMEDOUT crash on uat  
- Skill: redis-connectivity-probe  
- GDD Domain: Core systems resilience
