# Wave 5 — Volo (Narrative Dev)

**Task:** Issue #67 Clickable Exits in Narrative Panel — PR #86 to dev

**Outcome:** SUCCESS

**Work:**
- Exit detection uses server hints (`RoomHeaderMessage.exits`), not pure regex
- Eliminates false positives from LLM prose (e.g., "northern wind" ≠ exit)
- Terminal component accepts `availableExits` and `onExitClick` props (backward-compatible)
- Exit detection applies to `room` and `header` message types only
- `DIRECTION_ALIASES` in `exit-detection.ts` — add new directions here (extensible)
- Standalone `ClickableExits` component for contexts without server hints
- 28 tests passing
- PR #86 → dev

**Decision Inbox Entry:** volo-clickable-exits.md — archived to decisions.md

**Cross-Team Impact:**
- Drizzt: Narrative panel receives `availableExits` from room state
- Jarlaxle: Toast pattern can signal exit clicks (UX feedback)
- Minsc: Exit link test patterns (role="link" on span with tabIndex=0)

**Status:** Clickable exits API locked, anticipatory tests passing.
