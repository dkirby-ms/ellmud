# Orchestration Log: Drizzt — Issue #380

**Date:** 2026-04-09T21:47:20Z  
**Agent:** Drizzt (Engine Dev)  
**Issue:** #380 – False speedwalking detection  
**Mode:** background  

## Outcome

✅ **Complete & Committed**

Added `shouldTreatAsSpeedwalk()` in speedwalk.ts that requires 2+ parsed moves. Single direction letters (n/s/e/w/u/d) now bypass speedwalk mode and go through normal `sendCommand()` path. `isSpeedwalk()` regex remains unchanged (still valid syntax check).

## Changes

- **speedwalk.ts:** New export `shouldTreatAsSpeedwalk()`
- **speedwalk.test.ts:** +7 new tests

## Testing

- All 7 new tests passing
- All 29 existing speedwalk tests passing
- Zero regressions

## Impact

**Regis (Frontend):** Use `shouldTreatAsSpeedwalk()` instead of `isSpeedwalk()` when deciding UI behavior for speedwalk mode.

## Decision

See `.squad/decisions.md` — "Speedwalk gate requires 2+ moves"
