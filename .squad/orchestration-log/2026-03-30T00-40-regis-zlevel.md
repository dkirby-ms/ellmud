# Orchestration Log: Regis — Ghost Room Z-Level Inflation Fix

**Timestamp:** 2026-03-30T00:40Z  
**Agent:** Regis (Frontend Dev)  
**Task:** Fix ghost room z-level inflation bug in useExplorationMap  
**Mode:** background

## Scope

Ghost rooms for up/down exits were being positioned at `parentPos.z ± 1`, which inflated floor bounds and caused all rooms on a floor to show incorrect ↑/↓ indicators. The FloorSelector would also display phantom floors that didn't actually exist.

## Outcome

✅ **SUCCESS**

**Changes:**
- Modified `packages/client/src/hooks/useExplorationMap.ts`
- Removed the ghost positioning block that placed up/down ghosts at inflated z-levels
- Modified `packages/client/src/components/RoomNode.tsx`
- Replaced the z-level badge with exit-based ↑/↓ indicators (purple, matching inter-floor stroke color)
- Rooms now show badges only if they have `up` or `down` in their exits array

**Test Results:** Build clean, 24 layout tests pass

**Verification:**
- Floor bounds are now accurate — only visited rooms contribute to min/max floor
- RoomNode displays exit-based badges instead of z-level badges
- Ghost rooms remain in `ghostRooms` for traversal but don't affect positioning
- All existing tests continue to pass

## Impact

- **useExplorationMap.ts:** Ghost positioning block removed
- **RoomNode.tsx:** Z-badge replaced with exit-based badges
- **useFloorFilter.ts, MapRenderer.tsx, MinimapWidget.tsx:** No changes needed
- **Map UI accuracy:** Floor selection and bounds now reflect actual layout, not inflated ghost positions

## Decision Filed

✅ `.squad/decisions/inbox/regis-zlevel-fix.md` — ready for merge

## Next Steps

- Map rendering now reflects true floor bounds
- No follow-up work required on client side
