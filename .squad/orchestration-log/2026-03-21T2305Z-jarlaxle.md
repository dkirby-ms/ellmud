# Orchestration Log — Jarlaxle

**Date:** 2026-03-21T23:05Z  
**Agent:** Jarlaxle (Game Systems Dev)  
**Wave:** Phase 1 UX Navigation — Should-Fix Sprint

## Assigned Work (Item 5)

✅ **Fix reconnection "Return to Refuge"** — Changed from LOGOUT dispatch to navigation in `packages/client/src/hooks/useShardConnection.ts`

## Files Changed

- `packages/client/src/hooks/useShardConnection.ts` — Modified `onReturnToRefuge` callback to use React Router `navigate('/refuge')` instead of LOGOUT dispatch

## What Was Fixed

**Issue:** The reconnection overlay's "Return to Refuge" button was dispatching LOGOUT, destroying the user's session.

**Solution:**
- Replaced `dispatch(LOGOUT)` with `navigate('/refuge')`
- User stays authenticated
- Shard room is left cleanly
- Token and playerId preserved in AppContext

## Rationale

- Returning to Refuge is a navigation event, not a session-ending event
- Users should not be forced to re-authenticate after a shard disconnect
- Each page manages its own Colyseus room lifecycle (established pattern)

## Decisions Documented

- **"Decision: 'Return to Refuge' navigates, does not log out"** — Navigation pattern for room switches and overlay dismissals

## Integration Notes

- ReconnectionOverlay component unchanged
- useReconnection hook unchanged
- ShardExploration page unchanged
- Pattern aligns with page-scoped room lifecycle decision

## Status

✅ COMPLETED — Reconnection UX fixed, user session preserved on return to Refuge.
