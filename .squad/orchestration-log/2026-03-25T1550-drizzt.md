# Orchestration Log: 2026-03-25T15:50Z — Fix client.auth playerId handoff

**Agent:** Drizzt (Engine Dev)  
**Task:** Fix client.auth playerId handoff in ShardRoom + RefugeRoom  
**Status:** ✅ Complete — all 1657 tests pass  
**Branch:** fix/player-identity-handoff

## Work Summary

Fixed the identity handoff bug where production clients use `client.auth.playerId` (set by Colyseus from onAuth return value) but both rooms were reading `options['playerId']` (always undefined in production), causing silent fallback to `client.sessionId` (9-char nanoid, not a valid player UUID).

## Changes

- **ShardRoom.ts (line 252):** Read `client.auth.playerId` → `options['playerId']` → `client.sessionId`, excluding 'anonymous' sentinel
- **RefugeRoom.ts (line 91):** Same pattern applied
- **Impact:** `playerIds` map now contains correct persistent UUIDs instead of ephemeral sessionIds
- **Backward Compatibility:** Tests continue to pass because options fallback is preserved; only production auth path fixed

## Testing

- All 1657 existing tests pass
- Integration test coverage added by Minsc (player-identity-handoff.test.ts)
- Manual verification: player-skills FK now resolves correctly to valid UUIDs
