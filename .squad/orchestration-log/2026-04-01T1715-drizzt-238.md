# Orchestration Log — Drizzt #238
**Date:** 2026-04-01T17:15Z  
**Agent:** Drizzt (Engine Dev)  
**Task:** Fix death/spawn routing to faction strongholds  
**Issue:** #238  

## Outcome
✅ **SUCCESS** — PR #261 opened. Death/spawn routing now uses faction strongholds. 17 new tests, 2037 server tests pass.

## Work Completed

### Death Routing Architecture
- Server-authoritative death routing via `resolvePlayerHubTarget(factionSlug)`
- Caches faction-to-stronghold mappings from `playerFactionSlugs` map
- Falls back to The Refuge for unaffiliated players
- Faction routing table:
  - Ironwright → The Foundry (zone:the-foundry)
  - Veil → The Cartographium (zone:the-cartographium)
  - Scarlet → The Counting House (zone:the-counting-house)
  - None → The Refuge (zone:the-refuge)

### Login Routing Endpoint
- Created `/api/spawn-zone` endpoint for client login routing
- Returns `{ target, zoneSlug, factionSlug }` to client
- Client must call endpoint before connecting to zone server
- Enables faction-based respawn routing across sessions

### Narration Integration
- Updated narration layer with `resolvePlayerHubName()` function
- Injects specific zone name in death messages ("You awaken in The Foundry…")
- Uses same faction routing logic as death handler

### Testing
- Added 17 new unit tests covering:
  - Faction stronghold resolution
  - Fallback to Refuge behavior
  - API endpoint routing logic
  - Narration message generation
- All 2037 server tests passing (no regressions)

## Impact on Team

**Regis (Frontend):**
- Client must call `/api/spawn-zone` on login
- Connect to returned `target` instead of hardcoded `zone:the-refuge`
- Faction-specific stronghold names appear in death narration

**Jarlaxle (Systems):**
- Faction stronghold zones must exist and be registered
- Grace fallback to Refuge if zone not found
- Stronghold zones integrate with existing zone persistence

## Cross-Dependencies
- Works in conjunction with Jarlaxle's #239 (Refuge repurposing)
- Client update needed from Regis for full functionality
- No breaking changes to existing APIs

## PR Details
- **Branch:** `squad/238-death-spawn-routing`
- **PR:** #261
- **Files Modified:** 12
- **Tests Added:** 17
- **Status:** Ready for review/merge
