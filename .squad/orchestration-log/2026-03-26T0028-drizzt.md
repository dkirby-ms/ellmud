# Orchestration Log: Drizzt (Engine Dev) — 2026-03-26T00:28Z

**Agent:** Drizzt  
**Task:** Fix snap-to-bottom in Refuge text area  
**Mode:** Background  
**Outcome:** SUCCESS  

## Work Completed

Fixed auto-scroll regression in Refuge narrative panel:
- **Root cause:** `useAutoScroll` hook was watching `.length` instead of array reference
- **Fix:** Changed dependency from `state.messages.length` to `state.messages` (one-liner)
- **Result:** Narrative pane now correctly snaps to newest message on every message arrival

## Build Status
✅ **Clean build** — no regressions, all 552 server tests passing

## Context
The Refuge layout reorganization placed the narrative feed in a center flex column. The scroll watcher was using `.length` property (a primitive) as dependency, which doesn't trigger on new array instances. Switching to the array reference itself ensures proper re-evaluation.

## Files Changed
- Modified: `packages/client/src/pages/Refuge.tsx` (dependency array in `useAutoScroll` hook)

## Pattern
This aligns with the ShardExploration scroll pattern established in #196 / #204. Refuge now uses the same `useAutoScroll(dependency)` semantics.
