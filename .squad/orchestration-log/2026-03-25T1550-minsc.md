# Orchestration Log: 2026-03-25T15:50Z — Identity handoff integration test

**Agent:** Minsc (Tester)  
**Task:** Write onAuth→onJoin identity handoff integration test  
**Status:** ✅ Complete — 1659 tests pass (11 new)  
**File:** packages/server/src/rooms/__tests__/player-identity-handoff.test.ts

## Work Summary

Created comprehensive integration test suite exercising the real auth-to-join pipeline that existing tests bypassed. Old tests used `connectTo(room, { playerId })` which never invoked `onAuth`, giving false green results while production auth (token-based) silently used sessionId.

## Test Suite (11 tests)

- **Real auth initialization** with AuthService + in-memory stores
- **Token flow verification** — only token passed, playerId extracted from client.auth server-side
- **Player UUID keying** — verifies `.players` and `.playerIds` maps use persistent UUIDs
- **Fallback chain validation** — confirms exclusion of 'anonymous' sentinel
- **Existing tests** — verifies options['playerId'] fallback still works for backward compatibility

## Key Finding

`client.auth` on SDK-side client is always `undefined` — `.auth` only exists on server-side `Client` object. Assertions must inspect server-side room state, not SDK client properties.

## Impact

- 1659 total tests pass
- Auth handoff path now covered; this bug class cannot recur silently
- Drizzt's fix verified end-to-end
